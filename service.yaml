apiVersion: v1
kind: Service

metadata:
  name: employee-service
  namespace: default

spec:
  selector:
    app: employee-backend

  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000

  type: LoadBalancer